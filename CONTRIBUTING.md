# 협업 규칙

> 짧은 기간에 5명이 서로 다른 기능을 동시에 만듭니다.
> 규칙의 목적은 격식이 아니라 **충돌 방지와 진행 상황 공유**입니다. 부담되면 언제든 Discussions에 수정 제안해 주세요.

## ⚡ 3줄 요약

1. 모든 작업은 **이슈 → 브랜치 → PR** 순서로. 이슈 없이 브랜치 파지 않습니다.
2. `main` 과 `dev` 에 **직접 push 금지**. 반드시 PR로 병합합니다.
3. 하루 작업을 마치면 **자기 이슈에 코멘트 한 줄**을 남깁니다.

---

## 1. 이슈

- 제목은 **대괄호로 종류를 앞에 붙입니다.** → `[작업] 폰(APK)으로 시연할 수 있게 만들기`

  | 머리말 | 쓰는 때 |
  | --- | --- |
  | `[기능]` | 없던 것을 새로 만들 때 |
  | `[작업]` | 이미 있는 것을 고치거나 다듬을 때 |
  | `[버그]` | 동작이 잘못됐을 때 |
  | `[논의]` | 결정이 필요할 때 (답이 정해지지 않은 것) |
  | `[테스트]` | 되는지 확인해 보는 것이 목적일 때 |

- **반쯤 진행된 작업도 이슈로 먼저 올립니다.** 이슈가 곧 진행 현황판입니다.
- 하나의 이슈는 **반나절 안에 끝나는 크기**로 자릅니다. 이틀짜리 이슈는 상태를 알 수 없어 쓸모가 없습니다.
- 트러블슈팅 이슈는 해결됐더라도 **닫지 말고 남겨두세요.** 발표 자료의 "문제 해결" 파트가 여기서 나옵니다.

### 템플릿

`.github/ISSUE_TEMPLATE/` 에 8종이 있습니다. 새 이슈를 만들면 골라서 쓰게 됩니다.

🚀 Feature Request · 🐛 Bug Report · 📝 Documentation · ♻️ Update & Refactor ·
🔍 Troubleshooting · ⚡ Performance Issue · 🛡️ Security Issue · 🚨 Incident Report

### 라벨

지금 저장소에 있는 라벨입니다. 실제로 쓰는 건 앞의 다섯 개뿐입니다.

| 라벨 | 쓰는 때 |
| --- | --- |
| `enhancement` | 기능 추가 · 개선. **가장 많이 씁니다** |
| `bug` | 잘못 도는 것 |
| `documentation` | 문서 작업 |
| `question` | 답이 필요한 것 |
| `help wanted` | 혼자 못 정하겠어서 팀의 판단이 필요할 때 (`question` 과 같이 붙습니다) |
| `duplicate` · `invalid` · `wontfix` · `good first issue` | 기본 제공. 거의 안 씁니다 |

> ⚠️ **템플릿이 붙이는 라벨은 대부분 저장소에 없습니다.** 템플릿들은 `feature`,
> `troubleshooting`, `update`, `performance`, `security`, `incident`, `critical` 을
> 요청하는데 만들어 둔 적이 없어서, 없는 라벨은 그냥 안 붙습니다. 그래서 **이슈를
> 만든 뒤 라벨을 손으로 붙여야 합니다.** (템플릿을 고치거나 라벨을 만들어 두면
> 해결되는데, 아직 정하지 않았습니다.)

---

## 2. 브랜치

```
main                   최종 · 발표 기준. 여기로 직접 push 금지
 └─ dev                통합 브랜치 (기본 브랜치). 모든 작업이 여기로 모입니다
     ├─ feat/comfyui-connect
     ├─ fix/native-svg-transform
     ├─ docs/readme-cleanup
     └─ chore/ui-polish
```

**작업은 항상 `dev`에서 브랜치를 따고, PR도 `dev`로 보냅니다.**

- 형식: `타입/짧은설명` — 설명은 영어 소문자와 하이픈으로 씁니다.
- 타입은 네 가지입니다.

  | 타입 | 쓰는 때 |
  | --- | --- |
  | `feat` | 기능 추가 |
  | `fix` | 버그 수정 |
  | `docs` | 문서 |
  | `chore` | 설정 · 빌드 · 정리처럼 동작이 안 바뀌는 것 |

- 병합된 브랜치는 바로 삭제합니다.
- 남의 브랜치에 직접 push 하지 않습니다. 필요하면 그 사람 PR에 코멘트로 요청하세요.

> 💡 브랜치는 **기능 단위**로 짓고 짧게 쓰고 버리세요. 개인 이름으로 브랜치를 만들어
> 오래 쓰면 `dev`와 점점 벌어져서 나중에 머지가 힘들어집니다.

### 작업 흐름

```bash
git switch dev
git pull                                   # dev 최신 상태로
git switch -c fix/feed-button-double-tap   # 이슈 하나당 브랜치 하나

# ... 작업 ...

npm run check                              # 타입 + lint 통과 확인
git add .
git commit -m "fix: 밥 주기 버튼 연타 시 포만도 중복 증가"
git push -u origin fix/feed-button-double-tap
```

GitHub에서 **`dev`로 가는 PR**을 올립니다.

### 남의 작업 가져오기

내 브랜치에서 작업하는 동안 `dev`가 바뀌었으면:

```bash
git switch dev && git pull
git switch fix/feed-button-double-tap
git merge dev                              # 충돌나면 여기서 해결
```

PR 올리기 전에 한 번 해주면 머지가 훨씬 수월합니다.

### 접은 실험은 태그로 남깁니다

병합하지 않기로 한 브랜치를 그냥 지우면 커밋을 되찾을 수 없습니다. 지우기 전에 아카이브 태그를 답니다.

```bash
git tag -a archive/<브랜치명> origin/<브랜치명> -m "왜 접었는지 + 무엇이 들어있는지"
git push origin archive/<브랜치명>
git push origin --delete <브랜치명>
```

- 브랜치 목록은 깨끗해지고, 커밋은 영구 보존됩니다.
- 되살릴 때는 `git branch <새이름> archive/<브랜치명>` 한 줄이면 됩니다.
- 태그 메시지에 **왜 접었는지**를 꼭 적습니다. 몇 주 뒤에 그 이유를 아무도 기억하지 못합니다.

| 태그 | 내용 | 접은 이유 |
| --- | --- | --- |
| `archive/scene-habitats-by-biome` | 서식지별 확장 씬 5종 (초원·숲·사막·하늘·극지), `pet-scene.tsx` +235줄 | 고도화 일정 없음 |
| `archive/painterly-experiment` | 회화체 렌더링 4단계(단색·붓결·선 제거+빛·파스텔) + 꽃밭 씬, `lib/paint.ts` 신규 | 제품에 적용하지 않기로 함 |

---

## 3. 커밋

```
타입: 한 줄 요약 (50자 이내, 명령형)

- 상세 내용이 필요하면 본문에
- 관련 이슈: #12
```

**예시**

```
feat: 얼굴 임베딩 코사인 유사도 Top-5 추출 구현
fix: 밥 주기 버튼 연속 클릭 시 포만도 중복 증가 수정
chore: 안 쓰는 품종 이미지 에셋 정리
docs: 발표용 아키텍처 다이어그램 추가
```

**규칙**

- 한 커밋에 **한 가지 변경**만. "이것저것 수정" 커밋은 되돌릴 수 없습니다.
- `.env`, API 키, 모델 가중치, 학습용 이미지 원본은 **절대 커밋하지 않습니다.** 시작 전에 `.gitignore`를 먼저 확인하세요.
- **왜 그렇게 했는지**를 본문에 적어두면 나중에 그 코드를 건드릴 사람이 덜 헤맵니다. 무엇을 바꿨는지는 diff 를 보면 알지만, 왜인지는 diff 에 안 남습니다.

---

## 4. 패키지 설치

Expo 관련 패키지는 `npm install` 대신 **`npx expo install`** 을 쓰세요.
SDK 57에 맞는 버전을 알아서 골라줍니다.

설치했으면 `package.json`과 `package-lock.json` **둘 다 커밋**하세요.

> 💡 **새 라이브러리를 넣기 전에 팀에 공유해 주세요.** Expo Go에는 정해진 네이티브 모듈만
> 들어있습니다. 지원하지 않는 라이브러리를 넣으면 앱이 아예 안 켜지고, 그때부터는
> 개발 빌드(`eas build`)를 만들어서 팀 전원이 재설치해야 합니다.

### package-lock.json 충돌이 났을 때

손으로 고치지 마세요. 이렇게 하면 됩니다:

```bash
git checkout --theirs package-lock.json   # dev 것을 그대로 가져오고
npm install                                # 내 package.json 기준으로 다시 생성
git add package-lock.json
```

---

## 5. Pull Request

- **빈 PR은 리뷰하지 않습니다.** PR 템플릿은 따로 없으니 본문에 이 셋을 적어주세요.
  - 무엇을 했나 · 왜 그렇게 했나
  - 어떻게 확인했나 (`npm run check`, 직접 눌러본 것)
  - 리뷰어가 특히 봐줬으면 하는 파일
- 제목은 커밋 규칙과 동일: `feat: 얼굴 임베딩 Top-5 추출 구현`
- 본문에 `closes #이슈번호`를 넣으면 병합 시 이슈가 자동으로 닫힙니다.
- **승인 필수는 걸지 않습니다.** 기간이 짧아 승인 대기가 그대로 병목이 됩니다.
  - 대신 PR을 올린 뒤 **팀 채팅방에 링크를 공유**하고,
  - **30분 안에 반대 코멘트가 없으면 본인이 병합**합니다.
  - 단, **공통 영역**(레이아웃 · 배포 · 환경 설정 · DB 스키마)은 **반드시 1명 이상의 확인**을 받습니다. 여기서 깨지면 5명이 전부 멈춥니다.
- PR이 500줄을 넘어가면 리뷰가 불가능합니다. 기능을 쪼개서 올리세요.

---

## 6. 코드 리뷰 코멘트

지적의 무게를 앞에 붙여주세요. 짧은 프로젝트에서는 **"이건 꼭 고쳐야 하는 건가?"를 되묻는 시간**이 가장 아깝습니다.

| 접두사 | 의미 | 대응 |
| --- | --- | --- |
| `[P1]` | 병합 전에 반드시 수정 | 고치고 병합 |
| `[P2]` | 고치면 좋지만 지금은 아니어도 됨 | 이슈로 옮기고 병합 |
| `[P3]` | 단순 의견 · 취향 | 무시해도 됨 |
| `[Q]` | 질문 | 답변만 |

**예시**

```
[P1] 여기서 이미지 리사이즈를 안 하면 3MB 사진 올릴 때 임베딩이 타임아웃 납니다.
[P3] 이 변수명은 breedScore가 더 읽기 좋을 것 같아요.
[Q] 이 코사인 유사도 임계값 0.7은 어떤 기준으로 정하셨나요?
```

- 사람이 아니라 **코드**에 대해 씁니다. "왜 이렇게 했어요?"보다 "이 부분은 이런 경우에 문제가 될 것 같은데 어떻게 생각하세요?"
- 칭찬 코멘트도 남기세요. 남의 코드를 실제로 읽었다는 가장 확실한 증거입니다.

---

## 7. 기록

- **Decision Log** — 기술 선택이나 기획 변경이 있으면 Discussions에 남깁니다.
  형식: `무엇을 정했는가 / 후보는 무엇이었는가 / 왜 이걸 골랐는가`
  멘토링에서 나온 피드백과 그에 따른 변경도 여기 기록합니다. **발표 때 "왜 이렇게 만들었나"에 대한 답이 전부 여기서 나옵니다.**
- **막히면 2시간 안에 공유합니다.** 혼자 붙잡고 있는 시간이 전체 일정에서 가장 큰 손실입니다. 이슈에 `help wanted` 라벨을 붙이거나 채팅방에 올려주세요.
